# Redis

Connection multiplexer, distributed locks, rate limiters, transient agent state and provider health cache. Redis accelerates and coordinates; it never becomes the permanent store. Package: StackExchange.Redis.
